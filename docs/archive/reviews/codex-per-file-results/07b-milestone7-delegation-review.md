- High: `M7.2` still contradicts itself on grant scope. The checklist says grants propagate only to the requesting child’s subtree, but the last test expects a grant for child A to be reusable by sibling B. The source-of-truth spec says subtree grants do **not** extend to siblings; only the user’s `[a] always` path creates a whole-tree grant ([fundamentals.md](/home/blake/projects/anothercodingagent/fundamentals.md#L970), [fundamentals.md](/home/blake/projects/anothercodingagent/fundamentals.md#L972)).

- High: `M7.1b` renames the spawn-time proactive grant field to `preauth`, but the main spec names this `preAuthorizedPatterns`. That leaves the milestone doc out of contract with the source spec and creates an avoidable API mismatch ([fundamentals.md](/home/blake/projects/anothercodingagent/fundamentals.md#L972)).

- Medium: `M7.1b` still omits the required `authority` narrowing override from `spawn_agent`. The source spec requires `spawn_agent` to accept both `allowed_tools` and `authority` overrides, both narrowing-only. As written, this step is still incomplete ([fundamentals.md](/home/blake/projects/anothercodingagent/fundamentals.md#L312)).

- Medium: There is still no explicit step/test proving that non-delegating profiles (`researcher`, `reviewer`) cannot call `spawn_agent`. The profile model includes `Can delegate` as a hard distinction, but this file never validates enforcement of that rule ([fundamentals.md](/home/blake/projects/anothercodingagent/fundamentals.md#L303), [fundamentals.md](/home/blake/projects/anothercodingagent/fundamentals.md#L305)).